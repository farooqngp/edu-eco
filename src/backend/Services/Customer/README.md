# Customer

Placeholder core-domain service — rename once the real product domain is known. Same
5-project Clean Architecture layout as Identity. Created in rollout Phase 7 (proves
outbox → RabbitMQ → Elasticsearch projection).
