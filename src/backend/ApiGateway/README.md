# ApiGateway

YARP reverse proxy — the sole client-facing entry point (see `docs/architecture/PLATFORM_ARCHITECTURE.md`'s
"YARP Gateway" section). Created in rollout Phase 4, wired to the Identity service first.
