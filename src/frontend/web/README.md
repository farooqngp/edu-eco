# web

Angular workspace (Nx-managed): apps/shell + libs/{core,shared,feature-*}. Created in
rollout Phase 5 via `npx create-nx-workspace` — not hand-scaffolded, so the generated
structure matches Nx's own conventions exactly.
