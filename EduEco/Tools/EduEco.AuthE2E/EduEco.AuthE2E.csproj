<Project Sdk="Microsoft.NET.Sdk">

  <!--
    End-to-end authentication/authorization checks against the running Docker stack (docker-compose.yml).
    Development tool only: it trusts self-signed certificates on loopback hosts. Never point it at production.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Infrastructure\Infrastructure.csproj" />
  </ItemGroup>

</Project>
