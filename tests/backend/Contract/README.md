# Contract

API/event contract tests between services (e.g. a published integration event's schema
matches what `EduEco.Contracts` declares). Created once at least two services exist to
have a contract between (Phase 7+).
